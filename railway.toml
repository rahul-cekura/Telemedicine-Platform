[build]
builder = "DOCKERFILE"
dockerfilePath = "server/Dockerfile"
watchPatterns = ["server/**"]

[deploy]
startCommand = "node run-migrations.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
